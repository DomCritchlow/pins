# Copy to .env and fill in your values. .env is gitignored.
# Run `python3 scripts/build-config.py` to generate config.js from this file.

PINS_CLIENT_ID=XXXXX.apps.googleusercontent.com
PINS_API_KEY=XXXX
# Your Google email. Required — used to detect the admin (who sees the Admin
# panel + gets their own notebook auto-created) and to tell non-admins who to
# ask if their sheet hasn't been provisioned yet.
PINS_ADMIN_CONTACT=dominic@critchlow.us

# Optional — defaults to "PlaceTracker".
PINS_SHEET_NAME_PREFIX=PlaceTracker
